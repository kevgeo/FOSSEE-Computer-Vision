
//Reading grayscale image
I1 = imread("box.png");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Checking what happens when optional arguments are entered twice
//Perforing convolution
out1 = convolver(I1,"scalar",1,"size",3,"size",a);
//output->Please provide optional arguments only once

//Reading image
I2 = imread("lena.jpeg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Checking what happens when optional arguments are entered twice
//Perforing convolution
out2 = convolver(I2,"scalar",1,"size",3,"size",a);
//output->Please provide optional arguments only once


//Reading image
I3 = imread("clc1.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Checking what happens when optional arguments are entered twice
//Perforing convolution
out3 = convolver(I3,"scalar",1,"size",3,"size",a);
//output->Please provide optional arguments only once


//Reading grayscale image
I4 = imread("left01.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Checking what happens when optional arguments are entered twice
//Perforing convolution
out4 = convolver(I4,"scalar",1,"size",3,"size",a);
//output->Please provide optional arguments only once

