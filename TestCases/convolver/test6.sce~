
//Reading grayscale image
I1 = imread("box.png");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];
	
//Rearranging name value pair arguments and checking if they give correct output
//Perforing convolution
out1 = convolver(I1,"scalar",1,"values",a,"size",3);
//-> output is correct

//Reading image
I2 = imread("lena.jpeg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Rearranging name value pair arguments and checking if they give correct output
//Perforing convolution
out2 = convolver(I2,"scalar",1,"values",a,"size",3);
//-> output is correct


//Reading image
I3 = imread("clc1.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Rearranging name value pair arguments and checking if they give correct output
//Perforing convolution
out3 = convolver(I3,"scalar",1,"values",a,"size",3);
//-> output is correct


//Reading grayscale image
I4 = imread("left01.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Rearranging name value pair arguments and checking if they give correct output
//Perforing convolution
out4 = convolver(I4,"scalar",1,"values",a,"size",3);
//-> output is correct

