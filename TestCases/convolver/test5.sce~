
//Reading grayscale image
I1 = imread("box.png");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Perforing convolution
out1 = convolver(I1,"values",a,"ize",3,"sclar",1);
//output-> Wrong name-value argument pair has been entered by user


//Reading image
I2 = imread("lena.jpeg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Perforing convolution
out2 = convolver(I2,"values",a,"ize",3,"sclar",1);
//output-> Wrong name-value argument pair has been entered by user


//Reading image
I3 = imread("clc1.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Perforing convolution
out3 = convolver(I3,"values",a,"ize",3,"sclar",1);
//output-> Wrong name-value argument pair has been entered by user


//Reading grayscale image
I4 = imread("left01.jpg");

//Kernel or filter and convolution on image is done with this kernel
//This kernel will give same image with no change
a = [0 0 0; 0 1 0; 0 0 0];

//Perforing convolution
out4 = convolver(I4,"values",a,"ize",3,"sclar",1);
//output-> Wrong name-value argument pair has been entered by user


