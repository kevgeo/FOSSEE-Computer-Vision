function [fovx fovy focal ppoint apectRatio] = calibrationMatrixValues(camera,width,height,apertureWidth,apertureHeight)

	[fovx fovy focal ppoint apectRatio] = opencv_calibrationMatrixValues(camera,width,height,apertureWidth,apertureHeight)

endfunction
